const name="Anushka"
const repoCount= 100

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

//Always use to display using this backticks

const String1= String("A string object")

console.log(String1.charAt(2));
